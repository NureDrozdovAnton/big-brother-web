# Big Brother Web

Web-client for Big Brother project

## Setup

Initially create `.env.local` to specify env-variables (explained in `.env.template`) if needed.

```bash
# for local development
npm run dev
```

```bash
# for production build
npm run build

# distribution bundle to be found in ./dist
```
